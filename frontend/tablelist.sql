-- Teachers table moved to db/create_school_teachers_table.sql (creates `school_teachers`)
-- See the db/ directory for per-table SQL files.

-- Subjects table moved to db/create_school_subjects_table.sql (creates `school_subjects`)
-- See the db/ directory for per-table SQL files.

-- Grades table moved to db/create_school_grades_table.sql (creates `school_grades`)
-- See the db/ directory for per-table SQL files.

-- Rooms table moved to db/create_school_rooms_table.sql (creates `school_rooms`)
-- See the db/ directory for per-table SQL files.

-- Students table moved to db/create_school_students_table.sql (creates `school_students`)
-- See the db/ directory for per-table SQL files.

-- You can also create all tables at once with `db/create_all_tables.sql`
